import Select from './Select.vue'
import Option from './Option.vue'
import { withInstall } from '@create-element/utils'

export const CtSelect = withInstall(Select)
export const CtOption = withInstall(Option)

export * from './types'
